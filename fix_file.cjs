const fs = require('fs'); let s = fs.readFileSync('mangled_page.svelte', 'utf8'); const m1 = s.indexOf('<!-- Infinite Avatar Marquee -->\r\n    <div class="mt-2 border-t border-slate-100 dark:border-slate-800 pt-4">\r\n        </h2>'); if (m1 !== -1) { const m2 = s.indexOf('pointer-events-none">{\\('footer.momen_spesial') || 'Momen Spesial'}</h3>', m1); if (m2 !== -1) { const end = m2 + 96; const replacement = '      <div class="flex items-center justify-center -mb-6 relative z-20">\r\n        <h3 class="text-sm font-bold text-slate-400 dark:text-slate-500 uppercase tracking-widest pointer-events-none">{\\('footer.wajah_squad') || 'Wajah-wajah MAZEEDA Squad'}</h3>\r\n      </div>'; const fixed = s.substring(0, m1 + 109) + replacement + s.substring(end); fs.writeFileSync('src/routes/+page.svelte', fixed); console.log('Successfully fixed file'); } else { console.log('Could not find end chunk'); } } else { console.log('Could not find start chunk'); }
