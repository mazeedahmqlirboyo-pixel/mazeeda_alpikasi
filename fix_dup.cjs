const fs = require('fs'); let s = fs.readFileSync('src/routes/+page.svelte', 'utf8'); const m = s.indexOf('<!-- Landscape Image Carousel -->'); const m2 = s.indexOf('<!-- Landscape Image Carousel -->', m + 1); if (m2 !== -1) { const start = s.lastIndexOf('</h2>', m2); s = s.substring(0, start + 5) + s.substring(m2 - 4); fs.writeFileSync('src/routes/+page.svelte', s); console.log('Fixed duplication'); }
